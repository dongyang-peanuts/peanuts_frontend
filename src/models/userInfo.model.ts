export default interface AdminInfo {
  adminId: string;
  adminPwd: string;
}

export interface UserList {
  userKey: number;
  userEmail: string;
  userPwd: string;
  userAddr: string;
  userNumber: string;
  proNum: string;
  signupDate: string;
}
