export default interface AdminInfo {
  adminId: string;
  adminPwd: string;
}

export interface UserList {
  proNum: string;
  userAddr: string;
  userEmail: string;
  userNumber: string;
  userPwd: string;
}
